---
---

<div class="header-container">
  <h1><a href="/">蓼食う本の虫制作室</a></h1>
  <a
    href="https://forms.gle/c7Ls3MysYAQVouBQ7"
    target="_blank"
    rel="noopener noreferrer"
    class="contact-button"
  >
    <span class="contact-text">お問い合わせ</span>
  </a>
</div>

<style>
  .header-container {
    border-bottom: 2px solid var(--color-black);
    padding: 20px var(--space-gutter-lg);
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
    box-sizing: border-box;
    background-color: var(--color-white);
  }

  h1 {
    color: var(--color-black);
    font-weight: bold;
    font-size: 1.4rem;
    letter-spacing: 0.05em;
    margin: 0;
    min-width: 0;
  }

  h1 a {
    text-decoration: none;
    color: inherit;
  }

  h1 a:focus-visible {
    outline: 2px solid var(--color-black);
    outline-offset: 4px;
  }

  .contact-button {
    color: var(--color-white);
    background-color: var(--color-black);
    text-decoration: none;
    font-weight: bold;
    font-size: 0.95rem;
    white-space: nowrap;
    display: inline-block;
    padding: 10px 24px;
    cursor: pointer;
    flex-shrink: 0;
  }

  .contact-button:hover {
    background-color: var(--color-text-body);
  }

  .contact-button:focus-visible {
    outline: 2px solid var(--color-black);
    outline-offset: 4px;
  }

  .contact-text {
    line-height: 1.2;
  }

  @media screen and (max-width: 768px) {
    .header-container {
      padding: 16px var(--space-gutter);
      flex-direction: row;
      align-items: center;
    }

    h1 {
      font-size: 1rem;
      letter-spacing: 0.03em;
    }

    .contact-button {
      font-size: 0.85rem;
      padding: 8px 16px;
    }
  }
</style>
