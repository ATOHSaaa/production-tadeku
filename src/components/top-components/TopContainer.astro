<div class="top-container">
  <div class="top-grid-container">
    <div class="top-grid-copy-box">
      <p>
        つくる、<br />
        つたえる、<br />
        ひろげる。<br />
      </p>
    </div>
    <div class="top-grid-text">
      <p>
        蓼食う本の虫制作室は、Webを中心にさまざまなクリエイティブを提供する制作プロダクションです。<br /><br />
      </p>
      <p>
        デザイン、コーディング、写真撮影、文章執筆などを一気通貫で担当いたします。<br />
      </p>
      <a href="/about" class="about-link">
        <span class="about-button">
          当制作室について →
        </span>
      </a>
    </div>
  </div>
</div>

<style>
  .top-container {
    max-width: var(--width-content);
    margin: 0 auto;
  }

  .top-grid-container {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    margin-top: 80px;
    padding: 0 var(--space-gutter-lg);
    gap: clamp(24px, 5vw, 64px);
    align-items: start;
  }

  .top-grid-copy-box {
    font-size: clamp(2rem, 3.5vw + 0.5rem, 3.5rem);
    font-weight: bold;
    text-align: left;
    border-left: 4px solid var(--color-black);
    padding-left: 24px;
    width: fit-content;
    flex-shrink: 0;
  }

  .top-grid-copy-box p {
    writing-mode: vertical-rl;
    display: inline-block;
    text-align: left;
    line-height: 1.8;
    letter-spacing: 0.05em;
    margin: 0;
  }

  .top-grid-text {
    font-size: clamp(1rem, 1.5vw + 0.5rem, 1.25rem);
    font-weight: normal;
    line-height: 1.8;
    letter-spacing: 0.03em;
    padding-top: 4px;
    min-width: 0;
  }

  .about-link {
    text-decoration: none;
    color: inherit;
    display: block;
    width: fit-content;
    max-width: 100%;
    margin-top: 40px;
    margin-left: auto;
    margin-right: auto;
  }

  .about-link:focus-visible {
    outline: 2px solid var(--color-black);
    outline-offset: 4px;
  }

  .about-button {
    border: 2px solid var(--color-black);
    width: min(280px, 100%);
    text-align: center;
    padding: 14px 24px;
    font-weight: bold;
    background-color: var(--color-black);
    color: var(--color-white);
    font-size: 0.95rem;
    display: block;
    box-sizing: border-box;
  }

  .about-link:hover .about-button {
    background-color: var(--color-white);
    color: var(--color-black);
  }

  @media screen and (max-width: 900px) {
    .top-grid-container {
      grid-template-columns: 1fr;
      gap: 32px;
      padding: 0 var(--space-gutter);
    }

    .top-grid-copy-box {
      margin-inline: auto;
      border-left: none;
      padding-left: 0;
    }

    .top-grid-text {
      padding-top: 0;
    }
  }

  @media screen and (max-width: 480px) {
    .top-grid-copy-box {
      font-size: 1.75rem;
    }

    .top-grid-container {
      margin-top: 48px;
    }
  }
</style>
